# Local Data Directory

This directory is intentionally kept lightweight for GitHub.

Expected local-only folders:

```text
data/images/
data/output/
```

The full local dataset, generated model outputs, and image folders are ignored by git. See `docs/DATA.md` for record schemas and conversion commands.
